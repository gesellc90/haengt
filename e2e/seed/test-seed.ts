// Marker-Datei: das eigentliche Seed läuft via test-seed.mjs (vom globalSetup
// per `node` aufgerufen). TS sieht die .mjs nicht direkt — die Konstanten
// sind separat in `e2e/helpers.ts` definiert (Source-of-Truth für Spec-Code).
// Bei Änderungen an TEST_PASSWORDS müssen beide Dateien synchron gehalten
// werden.
export {};
